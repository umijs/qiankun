import type { ProxyOptions } from './types';
import type { Express } from '../compiled/express';
export declare function createProxy(proxy: {
    [key: string]: ProxyOptions;
} | ProxyOptions[], app: Express): void;
