{
  "name": "purehtml",
  "private": true,
  "version": "0.0.0",
  "description": "qiankun micro app with no build step: static html + vendored jQuery",
  "scripts": {
    "dev": "http-server . --cors -c-1 -p 7104"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
