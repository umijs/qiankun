// stands in for a vendor library (e.g. jQuery): later scripts must observe this global,
// exactly like the native "external scripts block later scripts" ordering guarantees
window.__CLASSIC_VENDOR__ = 'vendor-loaded';
