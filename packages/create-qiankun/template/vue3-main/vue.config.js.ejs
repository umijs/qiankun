const { defineConfig } = require("@vue/cli-service");
module.exports = defineConfig({
  transpileDependencies: false,
  lintOnSave: "warning",
  devServer: {
    port: "<%= port %>",
    open: true,
    client: {
      overlay: false
    }
  },

});
